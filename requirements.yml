collections:
  - name: community.general
  - name: redpanda.cluster
    type: galaxy
    # Pinned: an unpinned 'latest' means any bad collection release breaks
    # every CI lane immediately. Bump deliberately when a release ships.
    version: 0.12.0
  - name: ansible.posix
  - name: grafana.grafana
    version: 5.6.0
  - name: prometheus.prometheus

roles:
  - src: mrlesmithjr.mdadm
  - src: mrlesmithjr.squid
  - src: geerlingguy.node_exporter
