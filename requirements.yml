collections:
  - name: community.general
  # TEMP (DO NOT MERGE): points at the collection-hardening branch so CI can
  # validate this harness against the 0.13.0 candidate. Revert to the pinned
  # Galaxy release before merging.
  - name: https://github.com/redpanda-data/redpanda-ansible-collection.git
    type: git
    version: collection-hardening
  - name: ansible.posix
  - name: grafana.grafana
    version: 5.6.0
  - name: prometheus.prometheus

roles:
  - src: mrlesmithjr.mdadm
  - src: mrlesmithjr.squid
  - src: geerlingguy.node_exporter
